// Fix: Removed reference to "vite/client" to resolve "Cannot find type definition file" error.
// This is safe because the application no longer uses Vite-specific environment variables (`import.meta.env`).
